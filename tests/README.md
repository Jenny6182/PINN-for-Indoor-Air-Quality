### Tests

This directory contains preliminary tests used to verify that the initial experiment pipelines and configurations run successfully.

These tests are intended as basic pipeline checks rather than comprehensive unit or integration tests.
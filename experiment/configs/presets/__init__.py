"""
Preset experiment configurations with sensible defaults per pipeline.

Import a preset when you don't want to construct ExperimentConfig by hand:

    from experiment.configs.presets.simple import default_simple_config
    cfg = default_simple_config(dataset_path="data/datasets/...")
"""
